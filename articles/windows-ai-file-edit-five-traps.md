---
title: "Windows で AI にファイルを書き換えさせて踏んだ 5 つの罠——改行・混在・cp932・バックスラッシュ・アンカー"
emoji: "🪤"
type: "tech"
topics: ["Python", "Windows", "ClaudeCode", "Git", "技術記事"]
published: false
---


Windows で AI（Claude Code）にファイルを直させていて、同じ型の事故を 5 回踏みました。どれも**「差し替え対象の行だけ見れば安全」という前提を壊す**もので（全行が変わる・別の場所が変わる・器が止まる・半分だけ直る）、Windows 固有の話ではなく、Windows で実際に踏んだ「テキスト I/O と経路の既定値」の事故です。5 つとも小さいファイルで再現してから書いています。

![AI にファイルを書き換えさせて踏んだ 5 つの罠＝改行の正規化（1,412 行が全行 CRLF・緑のまま 8 時間）・混在の単一化（LF 379 行 → 0 行）・パイプの cp932・\b が 0x08・部分適用（半分だけ直ったファイル＝3 回とも書く前に止めた）。「差し替えた行だけ見れば安全」という前提が壊れる](/images/windows-ai-file-edit-five-traps/fig1_five_traps.png)

| # | 症状 | 隠れて変わったもの | 検出 | 防止 |
|---|---|---|---|---|
| ① | 1 か所足したら**全行が CRLF** に | 改行（LF → CRLF・1,412 行） | `rb` で改行を数える | `newline=""` で読み書き |
| ② | 混在改行のファイルに 1 行の編集 → LF が **0** に | 改行（LF 379 行 → 0） | 同上・混在なら手を止める | 混在を先に解消するか `rb`/`wb` で自分で書く |
| ③ | 印字の途中で器が止まる | 何も変わらない代わりに**止まる**（cp932） | `sys.stdout.encoding` | `PYTHONIOENCODING=utf-8` か `reconfigure` |
| ④ | 正規表現が二度と当たらない | `\b` が **backspace（0x08）** に | `od -c`・制御文字の grep | コードはファイルに直接書く・書いた後にバイトを見る |
| ⑤ | 途中まで書いてから失敗する（部分適用） | 半分だけ直ったファイル | assert の赤（3 回とも書く前に止まった） | **全部のアンカーが当たるまで 1 バイトも書かない** |

## ① テキスト I/O の改行正規化

Python の既定（`newline=None`）でファイルを読むと、改行は `\n` に正規化され、書くと実行環境の改行（Windows は `\r\n`）に変換されます（[公式](https://docs.python.org/ja/3/library/functions.html#open)）。つまり「読んで、1 か所直して、書き戻す」だけで、**触っていない行も全部 CRLF になる**。

実害: 納品物の JS 1,412 行にアンカー付きで数行足したところ、全 1,412 行が LF → CRLF。`node --check` は通り（CRLF は正しい JS）、E2E 92 断言も自作の検査 22 件も緑のまま、**8 時間**気づかれませんでした。いちばん怖いのは、ほとんど壊れなかったこと。

再現（5 行のファイル）:

```python
open("lf.txt", "wb").write(b"a\nb\nc\nd\ne\n")
s = open("lf.txt", encoding="utf-8").read()                      # 既定＝改行を \n に正規化
open("out.txt", "w", encoding="utf-8").write(s)                  # 既定＝\n を os.linesep に
# → CRLF 5・LF 0（全行が変わる）。newline="" で読み書きすると CRLF 0・LF 5（保たれる）
```

Unix でも CRLF のファイルを全文読み書きすれば逆向きに同じことが起きます。Windows 固有ではなく「既定のテキスト I/O」の仕様です。

## ② 混在改行は「単一化」される

改行が混在したファイル（CRLF 約 1.4 万行＋LF 379 行の記録ファイル）に、編集ツールで **1 行**差し替えたら、LF の 379 行が 0 になりました。純 LF のファイルは同じ編集で LF のまま。

最初は「多数派に揃えている」と思いましたが、再現すると違いました。CRLF 4 行＋LF 2 行を既定で読み書きすると **CRLF 6**＝**実行環境の改行に全部そろう**（多数派判定ではない）。当方の編集経路で何が起きたかは「混在が単一化された」までしか言えません。

防ぎ方は同じ: 編集の前に `rb` で CRLF と LF を数え、混在なら既定のテキスト I/O を通さない。

## ③ パイプ経路では stdout が cp932

Python は Windows のコンソールでは UTF-8 で印字しますが、パイプやリダイレクトなど非コンソール経路ではロケール側の文字コード（日本語 Windows では cp932）になり得ます（[公式](https://docs.python.org/3/library/sys.html#sys.stdout)）。AI がツール経由で Python を走らせる経路はまさにパイプ。「—」や絵文字を含む 1 行の印字で `UnicodeEncodeError` が出て、**器が途中で止まる**。

再現: 同じ経路で `PYTHONIOENCODING` を外して `print('—')` → `'cp932' codec can't encode character '\u2014'`。付けると `utf-8`。

これは「Windows の stdout は cp932」ではなく「**この経路では** cp932」です。防ぎ方は環境変数か、器の冒頭で `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`。

## ④ バックスラッシュが 1 段減って `\b` が backspace になる

正規表現 `\bTBD\b` を器に足したのに、二度と当たらない。`od -c` で見ると **`\b` が 0x08（backspace）**でした。

経路は「AI のツール → シェルのヒアドキュメント → Python の非 raw 文字列 → ファイルへ書く」。この経路では、**ヒアドキュメントの区切りを引用しても** `\\b` が `\b` になって届き、Python の非 raw 文字列が `\b` を backspace にしていました（再現 2/2＝引用あり・なしとも）。Bash のヒアドキュメント自体の仕様ではありません（引用した区切りなら展開しないのが仕様）。**当方の経路の観測**です。

防ぎ方: 正規表現を含むコードは**ヒアドキュメント越しに流さず、ファイルに直接書く**。書いた直後に `od -c` か、制御文字の grep でバイトを見る。

## ⑤ 途中まで書いてから失敗する（部分適用）

複数箇所を順に差し替える器は、3 か所目のアンカーが外れた時点で 2 か所目まで書き込み済み＝**半分だけ直ったファイル**が残ります。これが 5 つ目の罠で、防ぎ方は「全部のアンカーが 1 回ずつ当たることを確かめてから、1 回だけ書く」。この形にしておいたら、同じ日に 3 回、途中のアンカーが外れて止まり、**3 回ともファイルは無傷**でした（旧版に当てていた・写しと本文が違った・同じ文が 2 か所にあった）。罠を踏まなかった唯一の例なので、防波堤として載せます。

```python
for old, new in pairs:
    assert s.count(old) == 1, old[:40]   # 1 つでも外れたら、ここで止まる。まだ何も書いていない
    s = s.replace(old, new)
open(p, "w", encoding="utf-8", newline="\n").write(s)   # 全部当たってから 1 回だけ書く
```

## 書き換え前の点検（31 行）

上の ①〜④ を、書き換える前に 1 回見る小さな器を同梱します（依存なし）。改行の内訳（CRLF／LF／孤立 CR／混在）・制御文字・stdout の文字コードを印字し、混在・孤立 CR・制御文字のどれかがあれば exit 1。

```
$ python preflight_edit.py mixed.txt ctrl.txt
stdout: encoding=cp932 isatty=False PYTHONIOENCODING=None  ⚠ UTF-8 でない＝絵文字や「—」の印字で止まる
mixed.txt: 混在 CRLF=4 LF=2 孤立CR=0  ⚠ 混在＝既定のテキスト I/O で読み書きすると全行が実行環境の改行にそろう
ctrl.txt: LF CRLF=0 LF=1 孤立CR=0  ⚠ 制御文字 0x08（\b が backspace になっていないか）
```

器の中身は本家（Lab）に置いています。⚠ この器も最初は罠③を自分で踏みました（cp932 の経路で自分の「⚠」が印字できずに落ちた）。冒頭で自分の stdout を UTF-8 に切り替えてから印字する形に直しています。⚠ もう 1 つ、公開前の読者の再現で**孤立 CR（`\r` だけの改行）を「改行なし」と表示したまま exit 0 で通す穴**が見つかりました。既定のテキスト I/O は `\r` も改行として読むので混在と同じ脅威です。v2 で赤にしました（毒: `a\rb\r` → exit 1）。⚠ もう 1 つ、公開前の読者の再現で**孤立 CR（`\r` だけの改行）を「改行なし」と表示したまま exit 0 で通す穴**が見つかりました。既定のテキスト I/O は `\r` も改行として読むので混在と同じ脅威です。v2 で赤にしました（毒: `a\rb\r` → exit 1）。

`git diff --stat` は**煙探知器**です。「1 file changed, 3 insertions」のはずが行数が膨らんでいたら全行が変わっています。ただし Git は `core.autocrlf` や `.gitattributes` で改行を正規化するので、最終保証にはなりません。確定は raw bytes で数える方です。

## 言わないこと

- 「Windows が悪い」「Bash のヒアドキュメントが壊す」とは言わない。既定値と経路の話。
- 5 つは当方が踏んだ順で、網羅ではない。
- ④の 1 段減る場所は当方の経路の観測。経路が違えば起きない。

この記事の本家（再現の全出力・復旧手順・検証の記録）は Sumitsuke Lab → [Windows で AI にファイルを書き換えさせたら、触っていない行が変わった 5 つの型（検証の記録つき）](https://sumitsuke.jp/via/zenn/lab/file-edit-five-traps-windows/)。

### AI の利用について

事故を起こしたのも再現したのも AI（Claude Code）で、切り分け・再現の設計・公開の判断は人が行いました。案件由来の数字は行数だけで、ファイル名・内容・日付は書いていません。

### 関連

4 本の門の 1 つ目（ファイル変更の門）。次は結論の門 → [AI が「無い／できない」と書く前の 3 つ](https://sumitsuke.jp/via/zenn/lab/negation-needs-search-scope/)。

次に読む: [緑を信じない——器を信じる前に毒を 1 本](https://sumitsuke.jp/via/zenn/lab/green-is-not-proof/)
